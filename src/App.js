import './App.css';

 import { ReactDOM } from "react";
 import React from "react";
 

  function App() {
  return (root = ReactDOM.createRoot(document.getElementById('root')), render) => 
   (
      <React.StrictMode>
        <header />
      </React.StrictMode>
    );
    
   };

export default App;
